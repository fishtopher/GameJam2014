using UnityEngine;
using System.Collections;

public class rcProxyLayer : MonoBehaviour
{

    // Specify which level this layer proxies to - note that the layer names must match
    public string LevelToProxyTo = string.Empty;

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
